{
  "name": "@storiny/proto",
  "version": "0.0.0",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "lint": "buf lint src --config buf.yaml",
    "build": "yarn lint && yarn format && buf generate src --config buf.yaml --template buf.gen.yaml",
    "format": "buf format src -w --config buf.yaml"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.21.0-1",
    "@storiny/eslint-config": "^1.2.0",
    "@storiny/tsconfig": "^1.0.0",
    "eslint": "^8.51.0",
    "ts-proto": "^1.148.2",
    "typescript": "^5.2.2"
  }
}
