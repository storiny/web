import "server-only";

/**
 * Checks whether the user maintains a valid session and returns the user's ID.
 * TODO: Impl
 */
export const getUser = async (): Promise<string | null> =>
  "0000-0000-0000-0000";
