import "server-only";

import React from "react";

import GeneralUsePolicyClient from "./client";

const Page = (): React.ReactElement => <GeneralUsePolicyClient />;

export { metadata } from "./metadata";
export default Page;
