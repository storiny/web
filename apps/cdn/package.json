{
  "name": "@storiny/cdn",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up",
    "build": "rollup -c && docker build --platform linux/arm64 -t storiny_nginx nginx",
    "rollup": "rollup -c",
    "sync": "aws s3 sync buckets/base s3://base-3939361a --delete --exclude '*.DS_Store'",
    "sync:local": "cross-env AWS_ACCESS_KEY_ID=minio_user AWS_SECRET_ACCESS_KEY=minio_pass aws --endpoint-url=http://localhost:9000 s3 sync buckets/base s3://base-3939361a --delete --exclude '*.DS_Store'",
    "lint": "eslint \"**/*.{js,ts}\"",
    "test": "rollup -c && jest",
    "clean": "rm -rf nginx/njs-files"
  },
  "devDependencies": {
    "@babel/core": "^7.22.1",
    "@rollup/plugin-babel": "^6.0.3",
    "@rollup/plugin-commonjs": "^25.0.0",
    "@rollup/plugin-node-resolve": "^15.1.0",
    "@storiny/eslint-config": "*",
    "@storiny/prettier-config": "*",
    "@storiny/test-utils": "*",
    "@storiny/tsconfig": "*",
    "babel-preset-njs": "^0.7.0",
    "cross-env": "^7.0.3",
    "docker-compose": "^0.24.1",
    "dotenv-cli": "^7.2.1",
    "eslint": "^8.51.0",
    "jest": "^29.5.0",
    "njs-types": "^0.7.12",
    "prettier": "^3.0.3",
    "rollup": "^3.24.0",
    "rollup-plugin-uglify": "^6.0.4",
    "ts-jest": "^29.1.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@storiny/shared": "*"
  }
}
