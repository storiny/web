{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "**/.env.*local",
    ".env.local",
    ".env"
  ],
  "globalEnv": [
    "NODE_ENV"
  ],
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "build:prod": {
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**"
      ]
    },
    "test": {},
    "realms:test": {},
    "deploy": {
      "dependsOn": [
        "build",
        "test"
      ]
    },
    "lint": {}
  }
}
